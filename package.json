{
  "name": "gcloud-pubsub-helpers",
  "version": "1.0.0",
  "description": "",
  "main": "./build/test_cloud_func.js",
  "scripts": {
    "create_push_sub": "ts-node src/create_push_subscription.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gts": "^1.0.0",
    "typescript": "~3.4.0"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^0.29.1"
  }
}
